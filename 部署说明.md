# AITEK 前后端部署说明

适用于本仓库最外层：`AITEK_艾泰克`。

## 1. 架构与端口

```
浏览器
  │  开发：http://localhost:8081
  │  生产：http://服务器:80  （Nginx 托管 frontend/dist）
  ▼
前端 Vue2 + Element UI
  │  开发：vue-cli 把 /api、/media 代理到 8001
  │  生产：Nginx 把 /api 反代到 8001
  ▼
Django BFF  127.0.0.1:8001
  ├── Flask 功能用例微服务   127.0.0.1:8002   （需求生成用例 / RAG）
  └── Flask 接口执行微服务   127.0.0.1:8003   （单条/套件跑 HTTP）
  ▼
MySQL 库名 aitek（utf8mb4）
```

| 进程 | 目录 | 默认地址 | 作用 |
|---|---|---|---|
| 前端 | `frontend/` | `:8081`（开发） | 页面 |
| Django | `backend/` | `:8001` | 登录、接口管理、套件、报告等 API |
| 功能用例服务 | `ai-testcase-service/` | `:8002` | 需求文档生成功能用例 |
| 接口执行服务 | `ai-api-test-service/` | `:8003` | 接口用例实际发请求、断言 |

模型对话走 Django 里配置的 LLM（系统「模型配置」）。向量检索默认可走本机 Ollama `bge-m3:latest`（`http://127.0.0.1:11434`），按环境再改。

---

## 2. 环境要求

- Python 3.11+（当前开发环境为 3.13）
- Node.js 14+（Vue CLI 4）
- MySQL 8，库 `aitek`，字符集 `utf8mb4`
- 生产建议：Nginx + systemd + gunicorn
- 操作系统：macOS / Ubuntu 均可

数据库账号在 `backend/aitek_back/settings.py` 的 `DATABASES` 中配置。生产请改为环境变量或独立配置，不要把密码提交进 Git。

---

## 3. 本地开发启动

在仓库根目录分别开四个终端（顺序建议：先 MySQL，再 8002/8003，再 8001，最后前端）。

### 3.1 功能用例微服务（8002）

```bash
cd ai-testcase-service
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# 编辑 .env：DATABASE_URL、AI_SERVICE_INTERNAL_TOKEN（须与 Django 一致）
./venv/bin/python wsgi.py
```

健康检查：`GET http://127.0.0.1:8002/health`

### 3.2 接口执行微服务（8003）

```bash
cd ai-api-test-service
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
# 可选：.env 中配置 API_TEST_SERVICE_INTERNAL_TOKEN，须与 Django 一致
.venv/bin/python wsgi.py
```

健康检查：`GET http://127.0.0.1:8003/health`

### 3.3 Django（8001）

```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt gunicorn
# 可选：复制 .env.example 为 .env（runserver 默认不自动加载 .env，可 export 环境变量）
./venv/bin/python manage.py migrate
./venv/bin/python manage.py runserver 8001
```

常用环境变量（见 `backend/.env.example`）：

```
DJANGO_DEBUG=1
DJANGO_SECRET_KEY=...
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
DJANGO_CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8081,http://localhost:8081
AI_SERVICE_BASE_URL=http://127.0.0.1:8002
AI_SERVICE_INTERNAL_TOKEN=dev-internal-token
API_TEST_SERVICE_BASE_URL=http://127.0.0.1:8003
API_TEST_SERVICE_INTERNAL_TOKEN=
```

`AI_SERVICE_INTERNAL_TOKEN` 必须与 `ai-testcase-service/.env` 中相同，否则功能用例生成会鉴权失败。

### 3.4 前端（8081）

```bash
cd frontend
npm install
npm run serve
```

浏览器打开：http://localhost:8081/

开发代理在 `frontend/vue.config.js`：`/api/`、`/media/` → `http://127.0.0.1:8001`。

默认登录账号以库中用户为准（开发环境常见 `zhuchenyang` / `123456`）。登录接口走 AES，密钥前后端需一致（`AES_SECRET_KEY` 与 `frontend/src/utils/crypto.js`）。

---

## 4. 生产部署（Nginx + systemd）

当前脚本按服务器目录 `/opt/aitek`、用户 `ubuntu` 编写，模板在 `deploy/`。现成单元只覆盖 **Django 8001** 和 **功能用例 8002**。接口执行 **8003 需单独拉起**，否则「接口用例运行 / 套件执行」会失败。

更细的服务器操作也可对照 `deploy/README.md`。

### 4.1 代码放到服务器

```bash
ssh ubuntu@<服务器> 'sudo mkdir -p /opt/aitek && sudo chown ubuntu:ubuntu /opt/aitek'
# Git clone 或 rsync，排除 venv、node_modules、.env
```

### 4.2 装依赖并迁移

```bash
# Django
cd /opt/aitek/backend
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt gunicorn
cp .env.example .env   # 修改 SECRET、HOSTS、TOKEN
python manage.py migrate
python manage.py collectstatic --noinput

# 8002
cd /opt/aitek/ai-testcase-service
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt gunicorn
cp .env.example .env
# DATABASE_URL 建议指向 127.0.0.1

# 8003
cd /opt/aitek/ai-api-test-service
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt gunicorn
```

### 4.3 构建前端

```bash
cd /opt/aitek/frontend
npm ci
npm run build
# 产物：frontend/dist
```

### 4.4 systemd + Nginx

```bash
sudo cp /opt/aitek/deploy/systemd/aitek-ai.service /etc/systemd/system/
sudo cp /opt/aitek/deploy/systemd/aitek-django.service /etc/systemd/system/
sudo cp /opt/aitek/deploy/nginx/aitek.conf /etc/nginx/sites-available/aitek
sudo ln -sf /etc/nginx/sites-available/aitek /etc/nginx/sites-enabled/aitek
# 按实际 IP/域名改 nginx 的 server_name，以及 django 的 ALLOWED_HOSTS、CSRF
sudo nginx -t
sudo systemctl daemon-reload
sudo systemctl enable --now aitek-ai aitek-django
sudo systemctl reload nginx
```

- Django：gunicorn 绑定 `127.0.0.1:8001`，超时 600s（生成用例 SSE）
- 8002：gunicorn 超时 3600s（长 SSE）
- Nginx：静态页 `frontend/dist`；`/api/` 反代 8001 且 `proxy_buffering off`；`/media/`、`/static/` 指 Django 目录

安全组建议只开放 **80、22**，不要把 8001/8002/8003/3306 暴露到公网。

### 4.5 生产启动 8003（当前无现成 unit）

可用 gunicorn 常驻，例如：

```bash
cd /opt/aitek/ai-api-test-service
./.venv/bin/gunicorn -b 127.0.0.1:8003 -t 180 --workers 2 wsgi:app
```

建议仿照 `deploy/systemd/aitek-ai.service` 自建 `aitek-api-test.service`，`WorkingDirectory` 指向 `ai-api-test-service`，`EnvironmentFile` 配内部 token。Django 需设置：

```
API_TEST_SERVICE_BASE_URL=http://127.0.0.1:8003
API_TEST_SERVICE_INTERNAL_TOKEN=<与 8003 相同>
```

---

## 5. 发版更新

```bash
# 本机
git add -A && git commit -m "..." && git push

# 服务器
cd /opt/aitek && git pull
cd frontend && npm ci && npm run build
cd /opt/aitek/backend
./venv/bin/python manage.py migrate
./venv/bin/python manage.py collectstatic --noinput
sudo cp /opt/aitek/deploy/systemd/*.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl restart aitek-ai aitek-django
# 若有 8003 的 unit，一并 restart
```

功能用例生成是长连接。改完 `aitek-ai.service` 后必须 `daemon-reload` 再重启。若前端出现 `illegal chunk header` / 生成中途 500，先看：

```bash
journalctl -u aitek-ai -n 80 --no-pager
journalctl -u aitek-django -n 80 --no-pager
```

---

## 6. 启动后自检

| 检查 | 期望 |
|---|---|
| http://localhost:8081/ 或生产 80 端口 | 打开登录页 |
| `GET http://127.0.0.1:8002/health` | 200 |
| `GET http://127.0.0.1:8003/health` | 200 |
| 登录后打开接口管理 | 列表正常 |
| 需求管理「生成测试用例」 | 依赖 8002 |
| 接口用例「运行」、用例执行套件 | 依赖 8003 |

---

## 7. 目录对照

```
AITEK_艾泰克/
  frontend/                 Vue 前端
  backend/                  Django BFF
  ai-testcase-service/      8002
  ai-api-test-service/      8003
  rag/                      RAG 实现（Django 引用）
  deploy/                   Nginx / systemd 模板
  部署说明.md               本文件
```

`ApiTesting_sanitized/` 是独立教学项目，**不是**本系统运行依赖，部署 AITEK 时不必启动它。
